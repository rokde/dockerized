#!/bin/bash

. docker-images

docker run --rm \
    --interactive \
    --tty \
    --net host \
    --volume `pwd`:/opt \
    -w /opt \
    --user $(id -u):$(id -g) \
    --entrypoint composer \
    "$COMPOSER_IMAGE" "$@"
