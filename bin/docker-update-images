#!/bin/bash

. docker-images

echo "Updating all dockerized images"
docker pull $COMPOSER_IMAGE
docker pull $HTTPIE_IMAGE
docker pull $NODE_IMAGE
docker pull $PHP_IMAGE
docker pull $RANCHERIZE_IMAGE
docker pull $RANCHERIZE_LATEST
