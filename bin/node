#!/bin/bash

. docker-images

docker run --rm \
    --interactive \
    --tty \
    --net host \
    --volume `pwd`:/src:rw \
    --workdir /src \
    -e "PUID=$(id -u)" \
    -e "PGID=$(id -g)" \
    --entrypoint node \
    $NODE_IMAGE "$@"
