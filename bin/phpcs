#!/bin/bash

. docker-images

docker run --rm \
    --interactive \
    --name="phpcs" \
    --tty \
    --net host \
    --volume `pwd`:/app \
    --workdir /app \
    --user $(id -u):$(id -g) \
    --entrypoint phpcs \
    "$PHPCS_IMAGE" "$@"
